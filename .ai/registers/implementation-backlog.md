# Implementation Backlog

| ID | Deliverable | Acceptance evidence | Gate |
|---|---|---|---|
| B-001 | Deterministic lockfiles | Implemented; prove `npm ci` in clean API/APP jobs | G1 |
| B-002 | Route contract baseline | Implemented for 305 declarations plus 319 response call sites; three PDF-job routes and protected metrics metadata are additive | G0 |
| B-003 | API Vitest/Supertest foundation | Implemented 847-test suite with seeded authentication denial paths, processor-only token bootstrap, distributed payload-crypto replay/key recovery, reusable RBAC, registration/verification/reset controller contracts, atomically consumed one-time password-reset authorization, primary-bound transaction/history snapshots, cross-tenant Socket.IO denial plus Redis/user-room lifecycle, pooled Mongo URI/failure/ID-normalization behavior, race-safe API/worker startup and shutdown, tenant-scoped and protected-field post/comment/reaction/recursive-reply boundaries, inspection transaction/tenant behavior, upload tenant enforcement, tenant-safe notification delivery, 90%-gated tenant-scoped idempotency including processing races and disk-backed multipart hashing, work-request tenant/lifecycle/filter and omitted-remarks behavior, work-order/equipment/parts controller contracts, inventory stock/cycle-count/transfer/replenishment and lifecycle/failure behavior, equipment subtype/tenant behavior, work-order-template normalization/enrichment/tenant references, location hierarchy/tenant/transaction/cascade behavior, schedule and procedure enrichment/versioning/tenant-pinned mutations, fail-closed distributed scheduler execution, role administration permission ceilings, mapping and tenant denial paths, configuration/runtime/error boundaries, comprehensive local/S3/dual-read storage coverage, and rejected/timed-out dependency readiness behavior; controlled slice is 93.65% statements, 94.32% lines, 96.60% functions, and 81.30% branches with 93%/94%/96%/81% global gates and 90%+ sensitive-path gates; full repository measurement is 60.89% statements, 61.50% lines, 62.60% functions, and 50.52% branches and is ratcheted in CI; raise branch coverage to the planned 60% with seeded legacy-domain tests | G1 |
| B-004 | Ephemeral Mongo replica set | Implemented rollback integration test; add domain transactions | G1 |
| B-005 | Immutable API artifact | Implemented CI checksum/SBOM/provenance; prove staging deployment | G1 |
| B-006 | Validated centralized config | Implemented; all runtime environment reads now live in `configDB.ts` | G2 |
| B-007 | Request/trace propagation | Implemented across inbound HTTP, outbound processor calls, structured logs, and versioned domain-event jobs | G2 |
| B-008 | Structured telemetry | Pino, protected Prometheus, OTLP traces/metrics, CloudWatch log shipping, dashboards, alarms, and access-log retention are implemented as code; apply and prove alert delivery | G2 |
| B-009 | Production index control | Production `autoIndex=false`, 43-model manifest, create-only all-model migration, and specialized upload-metadata/quota/PDF-job migrations implemented; apply migrations and retain query-plan evidence | G2 |
| B-010 | Managed Redis integration | Distributed limiter/socket adapter implemented behind flags; two-instance proof remains | G3 |
| B-011 | BullMQ and outbox | Transaction-aware writer, exact-version consumer, idempotent email/notification producers, tenant-scoped processor workers, deterministic processor identities, retry/dead-letter/redrive, and additive asynchronous PDF request/status/signed-download lifecycle implemented; deploy the operations dashboard | G3 |
| B-012 | S3 storage migration | Private S3 driver, signed URLs, signature/malware gates, immutable tenant/checksum metadata, atomic tenant quota reservations, versioned indexes, dry-run/execute file and quota reconciliation, feature-flagged S3-first/local fallback, and tenant-checked report image reads implemented; execute staging reconciliation/migration, prove cutover, and retire fallback | G3 |
| B-013 | Angular destination-aware client | Implemented for all concrete primary CMMS callers through 88 registered compatibility routes: authentication/refresh/account lifecycle, posts/uploads/comments, schedules, complete work-order CRUD/status/comments/attachments/dashboard metrics, SOP/form-category CRUD, locations/floor maps/mappings, procedures, instructions/troubleshoot guides, assets/equipment, asset and location reports, asynchronous report PDFs, notifications, company/user administration/password/profile/roles/mail control, work-order templates, parts/types/import/history/transfer, observations, inspections, and work requests. Static checks prohibit concrete primary route literals outside the registry while preserving cache classifiers and API-base stripping; processor/logger/ML transports remain isolated. Unit/build/Playwright gates pass without UI or contract changes. | G3 |
| B-014 | Terraform multi-AZ platform | Terraform 1.15.8 format/provider validation passes for HA, WAF, CSP/security headers, encrypted log sinks, access evidence, dashboards/alarms, and scaling; plan review, backend states, and apply remain | G4 |
| B-015 | CodeDeploy blue-green | Configuration, deployment waiter, API readiness smoke, ordered SPA promotion, manual immutable-release rollback, and 90-day CI evidence retention implemented; execute staging traffic switch and rollback rehearsal | G4 |
| B-016 | Angular 15→22 sequence | build/unit/e2e/bundle/route/visual gate at every major | G5 |
| B-017 | Recovery and capacity certification | SLO, load, failover, restore and incident evidence | G6 |
