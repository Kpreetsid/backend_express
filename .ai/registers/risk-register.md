# Prioritized Risk Register

| ID | Priority | Risk | Evidence | Treatment / acceptance criterion | Owner | Due |
|---|---|---|---|---|---|---|
| R-001 | P0 | Full-repository API branch coverage is below the initial enterprise target | 847 tests now cover the sensitive boundaries plus primary-bound transaction/history snapshots, cross-tenant Socket.IO denial, pooled Mongo and race-safe process lifecycle, work-order, inventory lifecycle/failure paths, equipment, work-order-template, location hierarchy, procedure tenant/version behavior, and fail-closed distributed scheduler execution. The independent all-source measurement is 60.89% statements, 61.50% lines, 62.60% functions, and 50.52% branches, so statement/line/function baselines have crossed 60% but remaining legacy decision paths are demonstrably not seeded end to end. | Keep the current all-source ratchet and add seeded integration suites until branch coverage also meets the planned initial 60%, then ratchet all metrics toward 80% | QA/API | G1 |
| R-002 | P0 | Unsupported/vulnerable frozen frontend stack | Baseline: 94 findings (4 critical, 67 high). Current production tree: 22 findings (0 critical, 18 high, 3 moderate, 1 low) after compatible transitive fixes, including a patched CommonJS-compatible `brace-expansion`; the remainder require Angular-, ECharts-, or Form.io-breaking major changes. | Time-boxed exception expires 2026-10-26; critical remains zero; named governance approval is required and framework change requires user approval | Web/Security | 2026-10-26 |
| R-011 | P0 | Full-repository APP coverage is below the initial enterprise target | The complete 601-test Karma run measures 24.26% statements, 24.48% lines, 22.41% functions, and 14.58% branches. Authentication/session/cache files have dedicated 75-100% gates; the asset-report editor, work-order creation, bulk work-order wizard, and pump fleet overview have independent high-volume domain gates, but most legacy components and services remain below the planned 60% repository baseline. | Preserve the repository ratchet and add behavior tests by module without excluding legacy source from the denominator; reach 60% before certification | QA/Web | G1 |
| R-003 | P0 | Production state cutover is incomplete | Redis/S3/queue code plus tenant-checked report reads, atomic upload quota accounting, an S3-first/local fallback, and checksum/quota reconciliation exist, but managed services, staged reconciliation/migration, fallback retirement, and multi-instance evidence do not | Apply quota indexes, run maintenance-mode quota reconciliation, and provision S3 plus managed Redis before production ASG cutover; zero-failure file/quota reconciliation and fallback retirement before certification | Platform | G3 |
| R-004 | P1 | Immutable deployment is not yet applied | CI and hooks deploy prebuilt `dist` plus production dependencies | Run a signed artifact through staging and prove no runtime compilation | Release | G1 |
| R-005 | P1 | Deployment rollback is not proven in AWS | CodeDeploy blue-green, target groups, alarms, deployment waiter/API smoke, ordered SPA promotion, manual immutable-release rollback, and evidence retention are defined and locally tested | Apply in staging and retain successful traffic-switch, smoke, alarm, and rollback evidence | Platform | G4 |
| R-006 | P1 | Dependency readiness is not proven against managed services | Bounded Mongo/Redis/queue/storage readiness is implemented and returns 503 for required unsafe dependencies | Exercise checks against managed staging services and alert delivery | SRE | G2/G3 |
| R-007 | P1 | Observability retention and access controls are not deployed | Source `console` use is blocked; redacted logs, OTLP, protected metrics, fixed PM2 paths, KMS-encrypted log groups, 400-day ALB access evidence, dashboards and alarms are provider-validated | Apply the restricted sinks and prove access, retention, trace search, and alert delivery in AWS | SRE/Security | G2 |
| R-008 | P1 | Production query plans are not evidenced | Production `autoIndex=false`, a generated 43-model manifest, and versioned upload-metadata/quota/PDF-job index migrations are enforced | Apply reviewed indexes and retain `explain` evidence for high-volume queries | Data | G2 |
| R-009 | P1 | Missing restore evidence | No managed-environment drill artifact exists | Quarterly restore proves RPO <=15 minutes and RTO <=60 minutes | Data/SRE | G4 |
| R-010 | P2 | Additive APP routes could bypass the typed client boundary | All current concrete primary callers use 88 registered compatibility paths with URL/filter/header/payload regression gates. Static checks reject concrete `/api/...` request literals outside the registry, while cache classifiers and API-base stripping remain non-request exceptions. ML chatbot/document routes remain owned by the isolated ML transport; notification Socket.IO remains notification-only and processor/logger/ML destinations remain isolated. | Register and contract-test every future primary route before use; never attach primary auth to unknown destinations | Web | G3 |

Risk acceptance uses the template in `templates/risk-acceptance.md`, names an
approver and owner, and expires automatically.
